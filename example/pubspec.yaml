name: example
description: Single item storage example project.

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  single_item_storage: ^1.0.4
  single_item_shared_prefs: ^0.0.3
  single_item_secure_storage:
    path: ../single_item_secure_storage

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true